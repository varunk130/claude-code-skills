---
name: web-artifacts-builder
description: Build multi-component claude.ai HTML artifacts using React, Tailwind CSS, and shadcn/ui. For complex artifacts requiring state management or routing.
---

# Web Artifacts Builder

Stack: React 18 + TypeScript + Vite + Parcel + Tailwind CSS + shadcn/ui. Init with init-artifact.sh, develop, bundle to single HTML with bundle-artifact.sh, share as artifact.
