---
name: pdf
description: Full PDF processing - read, extract text/tables, merge, split, rotate, watermark, create, fill forms, encrypt/decrypt, extract images, and OCR scanned PDFs.
---

# PDF Processing Guide

Uses pypdf (merge/split/rotate/encrypt), pdfplumber (text/table extraction), reportlab (create PDFs), pytesseract (OCR), and CLI tools (pdftotext, qpdf, pdftk).
