---
name: code-connect-components
description: Connects Figma design components to code components using Code Connect. Use when user says "code connect", "connect Figma to code", or wants to map design components to code implementations.
---

# Code Connect Components

Workflow: get_code_connect_suggestions (find unmapped components) -> scan codebase for matches -> present to user -> send_code_connect_mappings (batch create). Requires published components on Organization/Enterprise plans.
